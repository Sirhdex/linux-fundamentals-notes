## Concepts
- Another crucial element in web development is the communication between browsers and web servers
- Apache's true strength lies in its modularity—it can be customized and extended with various modules to perform specific tasks
- curl and wget are used for communication between web servers and the CLI


## Commands

## Examples
- curl -I http://localhost:8080 -- uses curl to verify the local apache page

## Answers
- http.server -p 8080
- php -s 127.0.0.1:8080